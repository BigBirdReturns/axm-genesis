# The 2026-07 release train — the report (train PR 100)

The owner's mandate: *"drive to 100, then we'll see what happened."* This is
what happened. Every lane RFC-first, one PR per item, every merge gated by
the driver's own runs, every delegated ruling recorded where the owner can
overrule it. Linked from `docs/CONTINUITY.md`'s record.

## The lanes, as shipped

| Train items | Lane | Repo | GitHub PRs | RFC |
|---|---|---|---|---|
| 032–040 | Guild Hall (ledger made browsable) | arc | #68–#75 | arc `docs/RFC_GUILD_HALL.md` |
| 041–050 | Expansion Archive (union journey view) | arc | #76–#77, #79–#86 | arc `docs/RFC_EXPANSION_ARCHIVE.md` |
| 051–060 | Appliance expansion (any cartridge, one boot path) | world | #83–#92 | world `docs/design/RFC_APPLIANCE_EXPANSION.md` |
| 061–070 | Workshop conformance (the author sees what the tests see) | arc | #97–#106 | arc `docs/RFC_WORKSHOP.md` |
| 071–080 | Cartridge Library custody (identity made visible) | arc | #87–#96 | arc `docs/RFC_CARTRIDGE_LIBRARY.md` |
| 081–090 | Family doctrine & doc-truth (the law book made real) | genesis (+arc #107) | #27–#32 | `docs/RFC_FAMILY_DOCTRINE.md` |
| 091–100 | Quality / governance / release | genesis (+arc #108) | #33–#34 | `docs/RFC_TRAIN_RELEASE.md` |

## What the numbers did

| Repo | Suite at train start | At 100 | Gates |
|---|---|---|---|
| arc | 498 | **559** (+5 drills, all `errs: []`) | driver-local through 091; **CI floor added at 092** |
| world | 600 (at lane start) | **626** (+2 e2e specs, desktop+mobile) | `npm run check` + Playwright + CI per merge |
| genesis | 299 | **302** (+ doc-truth sweep: 34 pointers, 0 findings) | `make test` + guards |

## What became true that wasn't

- The **Guild Hall** and **Expansion Archive** read arc's campaign ledger as
  browsable history — union of library and ledger, artifact-missing states
  honest, read-only proven by drill (`ledgerUnchanged` on every run).
- The **Workshop** playtests through the same conformance harness the
  cartridge tests run — bounded, seeded, labeled "not certification."
- The **Library and the bay both name their digests**, preflight their
  imports, and receipt their exports; arc's author-side digest and world's
  bay digest were proven byte-identical end-to-end.
- **World's runtime is generic**: severed-march — never named in world's
  source — imported, booted, played, and recorded under arc's own pinned
  digest with zero runtime changes (world #92).
- The **law book exists** (`docs/CONTINUITY.md`), is guarded by test, and the
  operating doctrine (driver-model orchestration, RFC-first, stop/ask) is
  written law. A mechanical sweep proves no cross-repo doc pointer is a
  phantom.

## Delegated rulings awaiting the owner's ratify/overrule

1. **FIRST_CHARTER fresh boot 6 → 8 agents** (world RFC ruling 4) — the
   cartridge's own declaration now beats the engine default. Observable
   pacing change to the shipped game.
2. **"Dual-use pruning" resolved to doc-truth** (doctrine RFC ruling 1) — if
   the original intent targeted the ops line, that needs a broader mandate.
3. The lane-compression verdicts (059/069/079/086-087/093-098) — audits
   recorded instead of churn; spot-checkable via each RFC.

## Left deliberately for the owner

- **genesis PR #23** (LOCALIZATION + CARTRIDGE_LIFECYCLE doctrine) — parked
  for your review before this train began; never touched. arc's README now
  points at the PR honestly instead of at its unmerged files.
- **arc PR #78** — closed duplicate of #69 from the stranded prior session;
  only its stale branch remains (one-click delete on the PR page).
- **Engine observation** (world #88): resolving with a large roster chains
  open-ended relationship-tension drama cards that block the in-shell Leave
  button. Real engine behavior, out of every lane's scope, worth a look.
- World's desktop e2e carries **2 pre-existing skips** that predate the train.

## The two outages, for the record

The GitHub connector token expired twice across this effort: once stranding
the *previous* session at train item 034 (recovered by this one), and once
mid-092 (ridden out — receipts ran locally, merges landed on re-auth). The
doctrine's answer, now written: the drive continues locally; only the merge
waits.
