# Never rewrite bytes inside shards
shards/** -text
shards/**/*.txt -text

tests/vectors/** -text
tests/vectors/**/*.txt -text

# Common binary formats
*.parquet -text
*.bin -text
*.sig -text
*.pub -text
